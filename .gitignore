# ── Python ──
__pycache__/
*.py[cod]
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# ── uv / venv ──
.venv/
venv/
# uv.lock est versionné (application, pas librairie)

# ── Outils ──
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
.ipynb_checkpoints/

# ── Secrets & config locale ──
.env
.env.*
!.env.example
*.pem
credentials.json

# ── Données personnelles (cf. §10 : jamais publier) ──
data/eval/labels.jsonl
data/raw/
data/cv/
*.pdf
*.docx
!docs/**/*.pdf

# ── Modèles & caches lourds ──
# models/
# !db/models/
# !db/models/**
# .cache/
# *.onnx
# *.safetensors

# ── Postgres / Docker ──
pgdata/
*.dump


# ── IDE / OS ──
.idea/
.vscode/
!.vscode/settings.json
.DS_Store
Thumbs.db